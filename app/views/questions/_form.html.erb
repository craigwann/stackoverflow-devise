<%= render('errors', :object => @question) %>

<%= form_for @question do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <!-- <%= f.collection_select :user_id, User.order(:title),:id, :username %> -->

  <%= f.label :body %>
  <%= f.text_field :body %>

  <!-- <%= f.label :description %>
  <%= f.text_field :description %> -->

  <%= f.submit %>
<% end %>
